# chico-ai diff

Show a detailed diff of pending changes.

```bash
chico-ai diff
```

## Source filtering

Pass a source name to diff only that source:

```bash
chico-ai diff hooks
chico-ai diff steering-files
```

When omitted, all configured sources are diffed.
