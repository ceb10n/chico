# chico-ai apply

Apply the latest plan, writing files to the local provider target.

```bash
chico-ai apply
```

## Source filtering

Pass a source name to apply only that source:

```bash
chico-ai apply hooks
chico-ai apply steering-files
```

When omitted, all configured sources are applied.
